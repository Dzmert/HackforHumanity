# Placeholder defaults so the service boots before real secrets land.
# Real values are delivered by the platform to /run/base44/app.env (listed last
# in compose env_file, so they always win over anything here).
BASE44_API_KEY=
BASE44_APP_ID=
